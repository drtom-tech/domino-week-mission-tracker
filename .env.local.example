# Supabase Configuration
NEXT_PUBLIC_SUPABASE_URL=your_supabase_url
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_supabase_anon_key

# Development Mode Configuration
# Set to "true" to enable dev mode bypass
NEXT_PUBLIC_DEV_MODE=true

# Test User Configuration
# Create a dedicated test user in Supabase with this email for development
# IMPORTANT: Use a test email, NOT a real user email like drtom@graduatemedicine.com
# Note: DEV_TEST_USER_EMAIL is server-side only (for tasks.ts)
#       NEXT_PUBLIC_DEV_TEST_USER_EMAIL is client-side (for useAuth.ts)
DEV_TEST_USER_EMAIL=test-user@example.com
NEXT_PUBLIC_DEV_TEST_USER_EMAIL=test-user@example.com
